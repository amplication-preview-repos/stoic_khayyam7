/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { User } from "./User";
import { UserCountArgs } from "./UserCountArgs";
import { UserFindManyArgs } from "./UserFindManyArgs";
import { UserFindUniqueArgs } from "./UserFindUniqueArgs";
import { CreateUserArgs } from "./CreateUserArgs";
import { UpdateUserArgs } from "./UpdateUserArgs";
import { DeleteUserArgs } from "./DeleteUserArgs";
import { BusinessIncomeTaxFindManyArgs } from "../../businessIncomeTax/base/BusinessIncomeTaxFindManyArgs";
import { BusinessIncomeTax } from "../../businessIncomeTax/base/BusinessIncomeTax";
import { GstFindManyArgs } from "../../gst/base/GstFindManyArgs";
import { Gst } from "../../gst/base/Gst";
import { IndividualIncomeTaxFindManyArgs } from "../../individualIncomeTax/base/IndividualIncomeTaxFindManyArgs";
import { IndividualIncomeTax } from "../../individualIncomeTax/base/IndividualIncomeTax";
import { LegalComplianceFindManyArgs } from "../../legalCompliance/base/LegalComplianceFindManyArgs";
import { LegalCompliance } from "../../legalCompliance/base/LegalCompliance";
import { TdsFindManyArgs } from "../../tds/base/TdsFindManyArgs";
import { Tds } from "../../tds/base/Tds";
import { UserService } from "../user.service";
@graphql.Resolver(() => User)
export class UserResolverBase {
  constructor(protected readonly service: UserService) {}

  async _usersMeta(
    @graphql.Args() args: UserCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [User])
  async users(@graphql.Args() args: UserFindManyArgs): Promise<User[]> {
    return this.service.users(args);
  }

  @graphql.Query(() => User, { nullable: true })
  async user(@graphql.Args() args: UserFindUniqueArgs): Promise<User | null> {
    const result = await this.service.user(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => User)
  async createUser(@graphql.Args() args: CreateUserArgs): Promise<User> {
    return await this.service.createUser({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => User)
  async updateUser(@graphql.Args() args: UpdateUserArgs): Promise<User | null> {
    try {
      return await this.service.updateUser({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => User)
  async deleteUser(@graphql.Args() args: DeleteUserArgs): Promise<User | null> {
    try {
      return await this.service.deleteUser(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => [BusinessIncomeTax], {
    name: "businessIncomeTaxes",
  })
  async findBusinessIncomeTaxes(
    @graphql.Parent() parent: User,
    @graphql.Args() args: BusinessIncomeTaxFindManyArgs
  ): Promise<BusinessIncomeTax[]> {
    const results = await this.service.findBusinessIncomeTaxes(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [Gst], { name: "gsts" })
  async findGsts(
    @graphql.Parent() parent: User,
    @graphql.Args() args: GstFindManyArgs
  ): Promise<Gst[]> {
    const results = await this.service.findGsts(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [IndividualIncomeTax], {
    name: "individualIncomeTaxes",
  })
  async findIndividualIncomeTaxes(
    @graphql.Parent() parent: User,
    @graphql.Args() args: IndividualIncomeTaxFindManyArgs
  ): Promise<IndividualIncomeTax[]> {
    const results = await this.service.findIndividualIncomeTaxes(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [LegalCompliance], { name: "legalCompliances" })
  async findLegalCompliances(
    @graphql.Parent() parent: User,
    @graphql.Args() args: LegalComplianceFindManyArgs
  ): Promise<LegalCompliance[]> {
    const results = await this.service.findLegalCompliances(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @graphql.ResolveField(() => [Tds], { name: "tdsItems" })
  async findTdsItems(
    @graphql.Parent() parent: User,
    @graphql.Args() args: TdsFindManyArgs
  ): Promise<Tds[]> {
    const results = await this.service.findTdsItems(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
